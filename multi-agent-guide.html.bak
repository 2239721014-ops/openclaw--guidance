<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>多智能体互聊配置指南</title>
    <style>
        * { box-sizing: border-box; }
        :root {
            --primary: #5E5CE6;
            --primary-light: #A78BFA;
            --accent-green: #0D9488;
            --accent-green-light: #CCFBF1;
            --bg-main: #F5F5F7;
            --bg-card: #FFFFFF;
            --text-main: #1D1D1F;
            --text-secondary: #86868B;
            --shadow: 0 4px 24px rgba(94, 92, 230, 0.1);
            --radius: 16px;
            --radius-lg: 24px;
        }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'SF Pro Text', sans-serif; 
            background: var(--bg-main);
            color: var(--text-main);
            max-width: 800px; 
            margin: 0 auto; 
            padding: 40px 20px 80px; 
            line-height: 1.6; 
        }
        h1 { 
            font-size: 36px; 
            font-weight: 700;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent-green) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 12px;
        }
        h2 { font-size: 24px; font-weight: 600; margin-top: 40px; margin-bottom: 16px; }
        
        /* 导航栏 */
        .nav-bar { 
            position: sticky; 
            top: 20px; 
            background: rgba(255,255,255,0.85);
            backdrop-filter: blur(20px);
            padding: 16px 24px;
            border-radius: var(--radius-lg);
            margin-bottom: 32px;
            box-shadow: var(--shadow);
            border: 1px solid rgba(255,255,255,0.5);
            z-index: 100;
        }
        .nav-title { font-size: 12px; font-weight: 600; color: var(--text-secondary); margin-bottom: 12px; }
        .nav-grid { display: flex; flex-wrap: wrap; gap: 8px; }
        .nav-btn { 
            background: linear-gradient(135deg, rgba(94, 92, 230, 0.1) 0%, rgba(52, 211, 153, 0.1) 100%);
            color: var(--primary);
            border: 1px solid rgba(94, 92, 230, 0.2);
            padding: 10px 18px;
            border-radius: 20px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.25s;
            text-decoration: none;
        }
        .nav-btn:hover { 
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            color: white;
            transform: scale(1.02);
        }
        
        /* Agent 列表 */
        .agent-input-group {
            background: var(--bg-card);
            padding: 24px;
            border-radius: var(--radius-lg);
            margin: 20px 0;
            box-shadow: 0 2px 12px rgba(0,0,0,0.04);
        }
        .agent-list {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }
        .agent-item {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        .agent-item .num {
            width: 28px;
            height: 28px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            font-size: 14px;
            flex-shrink: 0;
        }
        .agent-item input {
            flex: 1;
            padding: 10px 14px;
            border: 2px solid rgba(94, 92, 230, 0.2);
            border-radius: 10px;
            font-size: 15px;
            transition: all 0.2s;
        }
        .agent-item input:focus {
            outline: none;
            border-color: var(--primary);
        }
        .agent-item .remove-btn {
            width: 28px;
            height: 28px;
            background: rgba(239, 68, 68, 0.1);
            color: #EF4444;
            border: none;
            border-radius: 50%;
            cursor: pointer;
            font-size: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s;
        }
        .agent-item .remove-btn:hover {
            background: #EF4444;
            color: white;
        }
        .add-btn {
            margin-top: 12px;
            padding: 10px 18px;
            background: rgba(94, 92, 230, 0.1);
            color: var(--primary);
            border: 2px dashed rgba(94, 92, 230, 0.3);
            border-radius: 10px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.2s;
            width: 100%;
            pointer-events: auto;
            position: relative;
            z-index: 10;
        }
        .add-btn:hover {
            background: rgba(94, 92, 230, 0.15);
            border-color: var(--primary);
        }
        .sync-btn {
            display: block;
            width: 100%;
            margin-top: 16px;
            padding: 14px 24px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.25s;
            pointer-events: auto;
            position: relative;
            z-index: 10;
        }
        .sync-btn:hover {
            transform: scale(1.02);
            box-shadow: 0 4px 12px rgba(94, 92, 230, 0.3);
        }
        
        /* 卡片 */
        .card { background: var(--bg-card); border-radius: var(--radius-lg); padding: 24px; margin: 16px 0; box-shadow: 0 2px 12px rgba(0,0,0,0.04); }
        .step { background: var(--bg-card); padding: 18px 20px; margin: 12px 0; border-radius: var(--radius); border-left: 4px solid var(--primary); position: relative; }
        .step-num { font-weight: 600; color: var(--primary); margin-right: 12px; background: var(--accent-green-light); padding: 4px 10px; border-radius: 8px; }
        
        /* 代码 */
        code { background: rgba(94, 92, 230, 0.08); padding: 3px 8px; border-radius: 6px; font-family: 'SF Mono', Monaco, monospace; color: var(--primary); }
        pre { background: #1E1E2E; color: #CDD6F4; padding: 20px; border-radius: var(--radius); overflow-x: auto; position: relative; }
        
        /* JSON 配置区 */
        .config-section {
            margin: 16px 0 16px 20px;
            position: relative;
        }
        .config-code {
            margin: 0;
            padding-top: 40px;
        }
        
        /* 复制按钮 */
        .copy-btn {
            position: absolute;
            top: 12px;
            right: 12px;
            background: linear-gradient(135deg, var(--accent-green) 0%, #10B981 100%);
            color: white;
            border: none;
            padding: 8px 14px;
            border-radius: 8px;
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.25s;
            z-index: 10;
        }
        .copy-btn:hover {
            transform: scale(1.05);
        }
        
        /* 提示词框 */
        .prompt-box { 
            background: linear-gradient(135deg, #1E1E2E 0%, #2D2B42 100%); 
            color: #E0E0E0; 
            padding: 28px; 
            border-radius: var(--radius-lg); 
            margin: 24px 0; 
            position: relative; 
        }
        .prompt-box h3 { color: #A78BFA; margin-top: 0; }
        .prompt-box .copy-btn {
            top: 20px;
            right: 20px;
            padding: 10px 18px;
            font-size: 14px;
        }
        
        /* 备注 */
        .note { background: var(--accent-green-light); padding: 16px 20px; border-radius: var(--radius); margin: 16px 0; border: 1px solid rgba(52, 211, 153, 0.3); }
        
        /* 动态标签 */
        .dynamic-tag {
            color: #EC4899;
            font-weight: 600;
            background: rgba(236, 72, 153, 0.1);
            padding: 2px 8px;
            border-radius: 4px;
        }
        
        .section { scroll-margin-top: 100px; }
        hr { border: none; height: 1px; background: linear-gradient(90deg, transparent, rgba(94, 92, 230, 0.2), transparent); margin: 48px 0; }
        
        .back-to-top {
            display: block;
            text-align: center;
            margin-top: 48px;
        }
        .back-to-top a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 500;
            padding: 12px 24px;
            background: linear-gradient(135deg, rgba(94, 92, 230, 0.1) 0%, rgba(52, 211, 153, 0.1) 100%);
            border-radius: 20px;
        }
        
        /* Toast */
        .toast {
            position: fixed;
            top: 20px;
            left: 50%;
            transform: translateX(-50%) translateY(-100px);
            background: linear-gradient(135deg, var(--accent-green) 0%, #10B981 100%);
            color: white;
            padding: 14px 28px;
            border-radius: 12px;
            font-weight: 600;
            box-shadow: 0 4px 20px rgba(13, 148, 136, 0.4);
            transition: transform 0.3s ease;
            z-index: 1000;
        }
        .toast.show {
            transform: translateX(-50%) translateY(0);
        }
    </style>
</head>
<body>
    <!-- Toast -->
    <div id="toast" class="toast">✓ 已复制!</div>

    <!-- 导航 -->
    <div class="nav-bar">
        <div class="nav-title">快速导航</div>
        <div class="nav-grid">
            <a href="#intro" class="nav-btn">简介</a>
            <a href="#config" class="nav-btn">配置</a>
            <a href="#steps" class="nav-btn">步骤</a>
            <a href="#prompt" class="nav-btn">提示词</a>
            <a href="#faq" class="nav-btn">FAQ</a>
        </div>
    </div>

    <h1 class="section" id="intro">多智能体互聊配置指南</h1>
    
    <p style="font-size: 17px; color: var(--text-secondary);">
        在同一个 OpenClaw Gateway 中实现多智能体互聊。通过 sessions_send 向目标 session key 投递消息，开启 agentToAgent 与 sessions.visibility = "all"，让多个 Agent 自动协作。
    </p>

    <!-- Agent 管理 -->
    <div class="agent-input-group">
        <div style="margin-bottom: 16px; font-weight: 600; color: var(--text-main);">
            🤖 填写你的智能体名称（已有或新建均可）
        </div>
        <div class="agent-list" id="agentList">
            <div class="agent-item">
                <span class="num">1</span>
                <input type="text" class="agent-name" placeholder="例如: alice, 小爱, helper" value="alice">
                <button class="remove-btn" onclick="removeAgent(this)" title="移除">×</button>
            </div>
            <div class="agent-item">
                <span class="num">2</span>
                <input type="text" class="agent-name" placeholder="例如: bob, 小美, responder" value="bob">
                <button class="remove-btn" onclick="removeAgent(this)" title="移除">×</button>
            </div>
        </div>
        <button class="add-btn" onclick="addAgent()">+ 添加更多智能体</button>
    </div>

    <!-- 智能体身份配置（可选） -->
    <div class="agent-input-group" style="background: linear-gradient(135deg, #f0f9ff 0%, #e0f2fe 100%); border: 2px dashed #0ea5e9;">
        <div style="margin-bottom: 16px; font-weight: 600; color: #0369a1;">
            🎯 智能体身份配置（可选）
        </div>
        <p style="font-size: 13px; color: #64748b; margin-bottom: 16px;">
            为每个智能体设置身份和使命，配置后会自动添加到提示词中
        </p>
        <div class="agent-list" id="identityList">
            <div class="agent-item">
                <span class="num">1</span>
                <input type="text" class="agent-identity-name" placeholder="智能体名称" value="alice" style="flex: 0.8;">
                <input type="text" class="agent-identity-role" placeholder="角色/使命 (例如: AI助手, 编程专家)" value="AI助手" style="flex: 1.2;">
                <input type="text" class="agent-identity-desc" placeholder="专长 (例如: Python, 硬件)" value="AI硬件和算力" style="flex: 1;">
            </div>
            <div class="agent-item">
                <span class="num">2</span>
                <input type="text" class="agent-identity-name" placeholder="智能体名称" value="bob" style="flex: 0.8;">
                <input type="text" class="agent-identity-role" placeholder="角色/使命" value="效率助手" style="flex: 1.2;">
                <input type="text" class="agent-identity-desc" placeholder="专长" value="日程管理" style="flex: 1;">
            </div>
        </div>
        <button class="add-btn" onclick="addIdentity()">+ 添加身份配置</button>
        <button class="sync-btn" onclick="syncAll()">🔄 同步更新</button>
    </div>

    <h2 class="section" id="config">JSON 配置</h2>
    <p style="color: var(--text-secondary);">点击复制，添加到 openclaw.json：</p>
    
    <div class="config-section">
        <button class="copy-btn" onclick="copyConfig()">📋 复制</button>
        <pre class="config-code" id="configJson">{
  "tools": {
    "agentToAgent": {
      "enabled": true,
      "allow": ["alice", "bob"],
      "maxPingPongTurns": 3
    },
    "sessions": {
      "visibility": "all"
    }
  }
}</pre>
    </div>

    <!-- 配置参数说明表 -->
    <div class="card" style="margin-top: 20px;">
        <h3 style="margin-top: 0; margin-bottom: 16px; color: var(--primary);">📋 配置参数说明</h3>
        <table style="width: 100%; border-collapse: collapse; font-size: 14px;">
            <thead>
                <tr style="border-bottom: 2px solid var(--primary);">
                    <th style="text-align: left; padding: 10px; color: var(--text-secondary);">配置项</th>
                    <th style="text-align: left; padding: 10px; color: var(--text-secondary);">推荐值</th>
                    <th style="text-align: left; padding: 10px; color: var(--text-secondary);">作用</th>
                </tr>
            </thead>
            <tbody>
                <tr style="border-bottom: 1px solid #eee;">
                    <td style="padding: 12px 10px;"><code>tools.agentToAgent.enabled</code></td>
                    <td style="padding: 12px 10px;"><code>true</code></td>
                    <td style="padding: 12px 10px;">开启跨 agent 通信</td>
                </tr>
                <tr style="border-bottom: 1px solid #eee;">
                    <td style="padding: 12px 10px;"><code>tools.agentToAgent.allow</code></td>
                    <td style="padding: 12px 10px;"><code>["agentA", "agentB"]</code></td>
                    <td style="padding: 12px 10px;">限定允许互通的 agent</td>
                </tr>
                <tr style="border-bottom: 1px solid #eee;">
                    <td style="padding: 12px 10px;"><code>tools.sessions.visibility</code></td>
                    <td style="padding: 12px 10px;"><code>"all"</code></td>
                    <td style="padding: 12px 10px;">允许跨 agent 可见 session</td>
                </tr>
                <tr>
                    <td style="padding: 12px 10px;"><code>tools.agentToAgent.maxPingPongTurns</code></td>
                    <td style="padding: 12px 10px;"><code>3</code> 或按需</td>
                    <td style="padding: 12px 10px;">控制自动来回回复轮数</td>
                </tr>
            </tbody>
        </table>
    </div>

    <!-- 目录隔离说明 -->
    <div class="card" style="margin-top: 20px; border-left: 4px solid #EF4444;">
        <h3 style="margin-top: 0; margin-bottom: 12px; color: #EF4444;">📁 目录隔离说明（重要）</h3>
        <p style="font-size: 14px; margin-bottom: 12px;">每个 Agent 需要三个独立隔离空间，<strong>不要共享目录</strong>：</p>
        <table style="width: 100%; border-collapse: collapse; font-size: 13px;">
            <tr style="border-bottom: 1px solid #eee;">
                <td style="padding: 8px;"><code>~/.openclaw/workspace-[名称]</code></td>
                <td style="padding: 8px; color: #666;">工作区（AGENTS.md, SOUL.md 等）</td>
            </tr>
            <tr style="border-bottom: 1px solid #eee;">
                <td style="padding: 8px;"><code>~/.openclaw/agents/[名称]/agent</code></td>
                <td style="padding: 8px; color: #666;">状态目录（auth-profiles.json）</td>
            </tr>
            <tr>
                <td style="padding: 8px;"><code>~/.openclaw/agents/[名称]/sessions</code></td>
                <td style="padding: 8px; color: #666;">会话存储（自动创建）</td>
            </tr>
        </table>
        <p style="font-size: 13px; color: #EF4444; margin-top: 12px; margin-bottom: 0;">
            ⚠️ <strong>官方警告：</strong>Never reuse agentDir across agents（会导致认证/会话混乱）
        </p>
    </div>

    <h2 class="section" id="steps">配置步骤</h2>

    <div class="step"><span class="step-num">1</span> 创建/确认智能体：<span class="dynamic-tag" id="step1">alice, bob</span></div>
    <div class="step"><span class="step-num">2</span> 在 openclaw.json 配置 agentToAgent 和 sessions.visibility（如上）</div>
    <div class="step"><span class="step-num">3</span> 给需要互聊的 agent 配 <code>tools.profile = "messaging"</code></div>
    <div class="step"><span class="step-num">4</span> <code>openclaw gateway restart</code></div>
    <div class="step"><span class="step-num">5</span> 让任一 agent 调用 sessions_send 向目标发消息</div>

    <div class="note">
        💡 <strong>说明：</strong>创建智能体时会自动创建 workspace，无需手动创建。
    </div>

    <hr>

    <h2 class="section" id="prompt">一键复制提示词</h2>
    <p>填写上方智能体名称后，点击"同步更新"，再复制：</p>

    <div class="prompt-box">
        <button class="copy-btn" onclick="copyPrompt()">📋 复制</button>
        <h3>多智能体互聊配置提示词</h3>
        <pre id="promptContent"># 任务：配置 OpenClaw 多智能体互聊

## 目标
在同一个 OpenClaw Gateway 中配置多个 AI Agent，让它们能够互相发送消息、进行多轮对话。

## 智能体列表
- alice
- bob

## 📁 目录隔离说明（重要）

每个 Agent 需要三个独立隔离空间，**不要共享目录**：

| 目录 | 作用 |
|------|------|
| `~/.openclaw/workspace-[名称]` | 工作区（AGENTS.md, SOUL.md, USER.md 等） |
| `~/.openclaw/agents/[名称]/agent` | 状态目录（auth-profiles.json, 模型配置） |
| `~/.openclaw/agents/[名称]/sessions` | 会话存储（自动创建） |

⚠️ **官方警告**：Never reuse `agentDir` across agents (会导致认证/会话混乱)

> `openclaw agents add [名称]` 会自动创建以上目录，无需手动创建。

## 执行步骤

### 第一步：创建智能体（如已有可跳过）
⚠️ **重要：创建前必须先检查是否已存在同名智能体！**
- 如果检测到名字相似或功能近似的智能体，**必须停下来询问用户**
- 询问用户：是否要创建新的同名智能体？
- 如果用户确认，需要让用户输入**新的区分名称**
- **绝对不能直接修改、覆盖或删除**任何已有文件

```
openclaw agents add alice
openclaw agents add bob
```
（创建时会自动创建 workspace，无需手动创建）
- 执行前先运行 `openclaw agents list` 确认没有同名智能体

### 第二步：配置 openclaw.json（每个 agent 加 messaging）
```json
{
  "tools": {
    "agentToAgent": {
      "enabled": true,
      "allow": ["alice", "bob"],
      "maxPingPongTurns": 3
    },
    "sessions": {
      "visibility": "all"
    }
  },
  "agents": {
    "alice": {
      "tools": { "profile": "messaging" }
    },
    "bob": {
      "tools": { "profile": "messaging" }
    }
  }
}
```

### 第三步：重启 Gateway
```
openclaw gateway restart
```

## 注意事项
1. sessionKey 格式：`agent:目标名称:session标签`
2. agentToAgent.allow 需包含所有需要通信的 agent
3. sessions.visibility 必须设为 "all"</pre>
    </div>

    <hr>

    <h2 class="section" id="faq">常见问题</h2>
    <div class="card">
        <div style="margin-bottom: 12px;"><strong>Q: 我已经有 1 个智能体了，怎么加第 2 个？</strong><br>→ 在上方列表添加第二个名称，然后执行 <code>openclaw agents add [名称]</code></div>
        <div style="margin-bottom: 12px;"><strong>Q: 可以添加第 3 个甚至更多吗？</strong><br>→ 可以！点击"添加更多智能体"，然后在 allow 数组中加入新名称</div>
        <div><strong>Q: 需要手动创建 workspace 吗？</strong><br>→ 不需要，创建智能体时 OpenClaw 会自动创建 workspace</div>
    </div>

    <div class="back-to-top">
        <a href="#" onclick="window.scrollTo({top:0,behavior:'smooth'}); return false;">↑ 返回顶部</a>
    </div>

    <script>
        let agentCount = 2;
        
        function addAgent() {
            agentCount++;
            const list = document.getElementById('agentList');
            const item = document.createElement('div');
            item.className = 'agent-item';
            item.innerHTML = `
                <span class="num">${agentCount}</span>
                <input type="text" class="agent-name" placeholder="智能体名称">
                <button class="remove-btn" onclick="removeAgent(this)" title="移除">×</button>
            `;
            list.appendChild(item);
        }
        
        function removeAgent(btn) {
            const items = document.querySelectorAll('.agent-item');
            if (items.length <= 2) {
                showToast('⚠️ 至少需要2个智能体');
                return;
            }
            btn.parentElement.remove();
            // 重新编号
            document.querySelectorAll('.agent-item').forEach((item, i) => {
                item.querySelector('.num').textContent = i + 1;
            });
            agentCount = items.length - 1;
        }
        
        function getAgentNames() {
            const names = [];
            document.querySelectorAll('.agent-name').forEach(input => {
                if (input.value.trim()) {
                    names.push(input.value.trim());
                }
            });
            return names;
        }
        
        function addIdentity() {
            const list = document.getElementById('identityList');
            const items = list.querySelectorAll('.agent-item');
            const num = items.length + 1;
            const item = document.createElement('div');
            item.className = 'agent-item';
            item.innerHTML = `
                <span class="num">${num}</span>
                <input type="text" class="agent-identity-name" placeholder="智能体名称" style="flex: 0.8;">
                <input type="text" class="agent-identity-role" placeholder="角色/使命" style="flex: 1.2;">
                <input type="text" class="agent-identity-desc" placeholder="专长" style="flex: 1;">
            `;
            list.appendChild(item);
        }
        
        function syncAll() {
            const names = getAgentNames();
            if (names.length < 2) {
                showToast('⚠️ 请至少填写2个智能体名称');
                return;
            }
            
            // 更新步骤中的显示
            document.getElementById('step1').textContent = names.join(', ');
            
            // 更新 JSON 配置
            const config = `{
  "tools": {
    "agentToAgent": {
      "enabled": true,
      "allow": ${JSON.stringify(names)},
      "maxPingPongTurns": 3
    },
    "sessions": {
      "visibility": "all"
    }
  }
}`;
            document.getElementById('configJson').textContent = config;
            
            // 获取身份配置
            const identities = [];
            document.querySelectorAll('.agent-item').forEach((item, i) => {
                const nameInput = item.querySelector('.agent-identity-name');
                const roleInput = item.querySelector('.agent-identity-role');
                const descInput = item.querySelector('.agent-identity-desc');
                if (nameInput && nameInput.value) {
                    identities.push({
                        name: nameInput.value,
                        role: roleInput ? roleInput.value : '',
                        desc: descInput ? descInput.value : ''
                    });
                }
            });
            
            // 更新提示词
            updatePrompt(names, identities);
            
            showToast('✓ 已同步更新!');
        }
        
        function updatePrompt(names, identities = []) {
            const namesList = names.map(n => `- ${n}`).join('\n');
            const agentsConfig = names.map(n => `    "${n}": {
      "tools": { "profile": "messaging" }
    }`).join(',\n');
            
            // 生成身份配置说明
            let identitySection = '';
            if (identities.length > 0) {
                identitySection = `
## 智能体身份配置（可选）
${identities.map((id, i) => `### ${id.name}
- **角色**: ${id.role || '(未设置)'}
- **专长**: ${id.desc || '(未设置)'}

在 workspace 中创建/更新 IDENTITY.md:
\`\`\`markdown
# IDENTITY.md - Who Am I?

- **Name:** ${id.name}
- **Creature:** ${id.role || 'AI助手'}
- **Vibe:** 资源、务实、高效
- **Emoji:** 🤖

## Notes
- 专注于服务用户
- 擅长 ${id.desc || '多领域知识'}
\`\`\``).join('\n\n')}
`;
            }
            
            const promptText = `# 任务：配置 OpenClaw 多智能体互聊

## 目标
在同一个 OpenClaw Gateway 中配置多个 AI Agent，让它们能够互相发送消息、进行多轮对话。

## 智能体列表
${namesList}
${identitySection}
## 执行步骤

### 第一步：创建智能体（如已有可跳过）
${names.map(n => `\`\`\`\nopenclaw agents add ${n}\n\`\`\``).join('\n')}
（创建时会自动创建 workspace，无需手动创建）

### 第二步：配置 openclaw.json（每个 agent 加 messaging）
\`\`\`json
{
  "tools": {
    "agentToAgent": {
      "enabled": true,
      "allow": ${JSON.stringify(names)},
      "maxPingPongTurns": 3
    },
    "sessions": {
      "visibility": "all"
    }
  },
  "agents": {
${agentsConfig}
  }
}
\`\`\`

### 第三步：重启 Gateway
\`\`\`
openclaw gateway restart
\`\`\`

## 注意事项
⚠️ **重要：避免重复创建智能体**
- 执行 `openclaw agents add` 前，**必须先检查是否已存在同名智能体**
- 如果检测到**名字相似**或**角色/功能近似**的智能体，**必须停下来询问用户**
- 询问用户：是否要创建新的同名智能体？如果用户确认，需要让用户输入**新的区分名称**
- **绝对不能直接修改、覆盖或删除**任何已存在的 workspace、agentDir 或 sessions
- 确认方式：执行 `openclaw agents list` 查看已有智能体列表

1. sessionKey 格式：\`agent:目标名称:session标签\`
2. agentToAgent.allow 需包含所有需要通信的 agent
3. sessions.visibility 必须设为 "all"`;

            document.getElementById('promptContent').textContent = promptText;
        }
        
        function copyConfig() {
            const text = document.getElementById('configJson').textContent;
            navigator.clipboard.writeText(text).then(() => {
                showToast('✓ JSON 配置已复制!');
            });
        }
        
        function copyPrompt() {
            const text = document.getElementById('promptContent').textContent;
            navigator.clipboard.writeText(text).then(() => {
                showToast('✓ 提示词已复制!');
            });
        }
        
        function showToast(message) {
            const toast = document.getElementById('toast');
            toast.textContent = message;
            toast.classList.add('show');
            setTimeout(() => {
                toast.classList.remove('show');
            }, 2000);
        }
        
        // 初始化
        syncAll();
    </script>
</body>
</html>
